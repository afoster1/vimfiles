" vim:fdm=marker

" Notes {{{
" * When the cursor is focused on a file path in a diff, use '<C-W>gf' to
"   navigate straight to the diff in a new tab.
" }}}

" 'gregsexton/gitv' plugin integration {{{

nmap <Leader>gv :Gitv<CR>
nmap <Leader>gV :Gitv!<CR>

" }}}
